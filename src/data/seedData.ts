import type { AccountId } from "../model/types.ts";

export const seedActuals: Record<AccountId, number> = {
  unit_price: 1000,
  quantity: 500,
  revenue: 500000,
  cogs: 300000,
  gross_profit: 200000,
  opex: 80000,
  operating_profit: 120000,
};
